<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Library validation matches Team IDs. Self-signed certs have no Team ID
         so the match always fails regardless of whether the library is signed.
         Mandatory for any self-signed binary with 'options' runtime.
     -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>